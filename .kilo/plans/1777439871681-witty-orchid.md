# Plan: Determine GPU Configuration for DepthCrafter's max_res=1024 Limit

## Goal
Answer the user's question: "What GPU configuration is this fixed code made for?" regarding the hardcoded 1024x1024 resolution cap in DepthCrafter to prevent OOM errors.

## Research Findings
- **Hardcoded Limit**: max_res = 1024 in both depthcrafter_logic.py (line 225) and depthcrafter_ui.py (line 516). This is a fixed safeguard, not dynamic.
- **VRAM Guide**: VRAM_USAGE_GUIDE.md indicates DepthCrafter requires significant VRAM:
  - 1024x1024 resolution uses ~8-12GB VRAM (estimated from context).
  - Higher resolutions (e.g., 4K) would exceed typical GPU limits.
- **GPU Detection**: depthcrafter_ui.py has auto-detection for offload_type based on GPU name and VRAM (lines 65-67), but this doesn't adjust max_res.
- **No Dynamic Adjustment**: The limit is static; no code adjusts 1024 based on available VRAM.
- **Assumed Hardware**: Based on documentation and typical usage:
  - Likely designed for GPUs with 12-16GB VRAM (e.g., RTX 3060/3070/3080, A5000).
  - Prevents OOM on mid-range GPUs while allowing 1024x1024 processing.

## Plan Steps
1. **Verify VRAM Calculations**: Cross-reference VRAM_USAGE_GUIDE.md with actual code for memory estimates at 1024x1024.
2. **Check for GPU-Specific Code**: Search for any conditional logic based on GPU model or VRAM that might inform the 1024 limit.
3. **Review Benchmarks**: Examine benchmark scripts for resolution tests and associated GPU requirements.
4. **Consult Documentation**: Analyze user guides for stated GPU recommendations.
5. **Propose Answer**: Synthesize findings into a response explaining the assumed GPU config (likely 12-16GB VRAM GPUs).

## Potential Outcomes
- If dynamic adjustment exists, recommend implementing VRAM-based scaling.
- If no adjustment, confirm it's for conservative mid-range GPUs to ensure stability.

## Questions for User
- Do you have access to the specific GPU used for testing this limit?
- Are you experiencing OOM at 1024x1024, or is the cap too restrictive for your setup?